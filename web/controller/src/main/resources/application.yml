server:
  port: 8080

spring:
    datasource:
        name: test
        url: jdbc:mysql://localhost:3306/spring-boot-example
        username: root
        password: 120157229
        driver-class-name: com.mysql.jdbc.Driver
        # -----数据库连接池信息----- #
        type: com.alibaba.druid.pool.DruidDataSource
        initialSize: 3
        minIdle: 3
        maxActive: 30
        maxWait: 15000
        timeBetweenEvictionRunsMillis: 120000
        minEvictableIdleTimeMillis: 300000
        validationQuery: SELECT 1 FROM DUAL
        testWhileIdle: true
        testOnBorrow: false
        testOnReturn: false
        poolPreparedStatements: false
        maxPoolPreparedStatementPerConnectionSize: 50
        # -----数据库连接池信息----- #
    redis:
          host: localhost
          database: 1
          port: 6379
          ssl: false
          timeout:
          lettuce:
            pool:
              min-idle: 3
              max-idle: 5
              max-wait: 60000ms
              max-active: 10

async:
    executor:
        coreSize: 10
        maxSize: 15
        queueCapacity: 999
        prefix: async-service-

mybatis:
  mapper-locations: classpath*:xml/*.xml
