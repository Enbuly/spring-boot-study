<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace value is the UserMapping interface-->
<mapper namespace="com.example.mapper.UserMapping">
    <!--id value should be a method name in User_Mapper interface-->

    <!--user data -->
    <select id="selectPasswordByUserName" resultType="String" parameterType="String">SELECT password FROM user WHERE name=#{name}</select>

</mapper>